package gr.blackswamp.diceroller.logic

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class MainViewModel(val app: Application) : AndroidViewModel(app), FragmentParent {
}